name: 🕸️ Daily Finance Monitor — Auto Deploy

on:
  # 每天早上8:00自动运行（中国时区 UTC+8）
  schedule:
    - cron: '0 0 * * *'   # UTC 00:00 = 北京时间 08:00
  # push 到 master 时也触发（方便手动测试）
  push:
    branches: [master]
    paths:
      - 'scripts/**'
      - 'run.sh'
      - 'web/**'
      - '.github/workflows/**'
  # 也支持手动触发
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

env:
  VERCEL_ORG_ID: team_UFlSZ6tKv1vWOOXYU0PlVdeT
  VERCEL_PROJECT_ID: prj_ZgDuvOfJxYOy4SFRnbtxMK4kLUu9

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: 📦 Install dependencies
        working-directory: ./
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 💰 Fetch data & generate report & push
        working-directory: ./
        run: |
          # 将项目根目录加入 Python 模块搜索路径
          export PYTHONPATH="$PWD:$PYTHONPATH"
          bash run.sh
        env:
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          WECOM_BOT_URL: ${{ secrets.WECOM_BOT_URL }}

      - name: 🚀 Deploy to Vercel (Production)
        working-directory: web
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npm install -g vercel@latest
          vercel pull --yes --environment=production \
            --token="$VERCEL_TOKEN"
          vercel deploy --prod \
            --token="$VERCEL_TOKEN"

      - name: ✅ Done
        working-directory: ./
        run: echo "✅ 部署完成！页面已更新 → https://web-sage-mu-13.vercel.app"
